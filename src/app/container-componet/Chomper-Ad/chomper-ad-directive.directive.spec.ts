import { ChomperAdDirectiveDirective } from './chomper-ad-directive.directive';

describe('ChomperAdDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChomperAdDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
